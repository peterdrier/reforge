// Satellite of _HarnessProbe.TwoSection.cs — see that file. The ".Camp" segment is what makes the
// harness compile this into assembly SampleSolution.Camp, i.e. section Camp.

namespace SampleSolution.Gate.Rules;

public interface ICampProbeService
{
    int Count();
}
