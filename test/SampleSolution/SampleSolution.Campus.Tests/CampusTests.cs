namespace SampleSolution.Campus.Tests;

// The substring trap: this project name CONTAINS "Camp" but does not NAME the Camp section — no
// dotted segment of it is "Camp". References two sections, so the tie-break runs and must decline.
public class CampusTests
{
    public void CampAndLodgeShareTheCampus()
    {
    }
}
